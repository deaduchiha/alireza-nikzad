import { defineStyle } from "@chakra-ui/react";

const name = defineStyle({
  color: "#212529",
  fontWeight: 900,
});

const job = defineStyle({
  color: "#343a40",
  fontWeight: 200,
});

export { name, job };
